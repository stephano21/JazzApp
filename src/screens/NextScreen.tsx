import React, {useState} from 'react';
import {BaseScreen} from '../templates/BaseScreen';
import {Text} from 'react-native';
import {SearchInput} from '../components/BaseComponents/SearchInput';
import {
  DynamicTable,
  TableColumn,
} from '../components/BaseComponents/DynamicTable';
import {ScrollView} from 'react-native-gesture-handler';

export const NextScreen = () => {
  //create a function that return a jsx element that render quotes, author must be in a bold text and black color, the quotes must render in a gray color

  
  return (
    <BaseScreen>
      <>
        
      </>
    </BaseScreen>
  );
};
